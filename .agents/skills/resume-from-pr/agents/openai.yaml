interface:
  display_name: "Resume from PR"
  short_description: "Resume interrupted work from a pull or merge request"
  default_prompt: "Use $resume-from-pr to continue the interrupted work on this pull request."
policy:
  allow_implicit_invocation: false
