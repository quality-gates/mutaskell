interface:
  display_name: "Resume from Pi"
  short_description: "Resume interrupted Pi agent work"
  default_prompt: "Use $resume-from-pi to continue the interrupted Pi session."
policy:
  allow_implicit_invocation: false
