interface:
  display_name: "Outpost Found"
  short_description: "Found an outpost — this session becomes prefect"
  default_prompt: "Use $outpost-found to found an outpost in this session."
policy:
  allow_implicit_invocation: false
