interface:
  display_name: "Resume from Claude"
  short_description: "Resume interrupted Claude Code work"
  default_prompt: "Use $resume-from-claude to continue the interrupted Claude session."
policy:
  allow_implicit_invocation: false
